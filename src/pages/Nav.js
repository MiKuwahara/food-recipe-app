import { Link } from 'react-router-dom';

function Nav() {
    return(
        <header>
            <nav>
                <Link to="/">Home</Link>
            </nav>
        </header>
    )    
}

export default Nav;